intent vote(votes: Count) {
    guard {
        votes >= votes_zero;
        old_votes == votes;
    }
    solve {
        priority: security;
        target: election_system;
    }
    verify {
        votes == old_votes;
    }
}
